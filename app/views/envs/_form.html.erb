<%= form_for(@env) do |f| %>
  <% if @env.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@env.errors.count, "error") %> prohibited this env from being saved:</h2>

      <ul>
      <% @env.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, rows: 6, cols: 40 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
